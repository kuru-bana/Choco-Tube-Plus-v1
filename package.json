{
  "name": "choco-tube-plus",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "start": "cd server && node server.js",
    "heroku-postbuild": "cd app && npm install && npm run build && cd ../server && npm install"
  }
}
